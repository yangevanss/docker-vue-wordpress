{% if menu %}
	<ul>
		{% for item in items %}
			<li class="{{ item.classes | join(' ') }}">
				<a target="{{ item.target }}" href="{{ item.link }}">{{ item.title }}</a>
				{% include "components/Menu/index.twig" with {'items': item.children} %}
			</li>
		{% endfor %}
	</ul>
{% endif %}
