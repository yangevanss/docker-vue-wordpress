{% extends "base.twig" %}

{% block style %}
    {{ enqueue_style('search', true) }}
{% endblock %}

{% block content %}
    <div class="search">
        <div class="container">
            {% include "components/molecules/breadcrumb.twig" %}
            <ul class="search__main">
                {% for post in posts %}
                    <li class="search__post">
                        <a 
                            href="{{ post.guid }}" 
                            v-bg="'{{ require_assets('img/block-map.jpg') }}'"
                        >
                            <p>
                                {{ post.post_title }}
                            </p>
                        </a>
                    </li>
                {% endfor %}
            </ul>
            {% include "components/molecules/pagination.twig" %}
        </div>
    </div>
{% endblock %}